# 用户管理功能测试

**优先级**: P1
**模块**: 用户管理
**前置条件**:
- 应用服务已启动（后端 http://localhost:8080，前端 http://localhost:3000）
- 数据库有测试账号：
  - admin / 123456（管理员）
  - security001 / 123456（安保人员）
  - buyer001 / 123456（买家）
- Redis 服务正常
- 已使用管理员账号登录
**预计耗时**: 180秒

---

# 第一部分：普通用户接口

---

## 用例1: 获取当前用户信息
**编号**: `user_info_get_normal_1`

### 步骤
1. 使用 admin/123456 登录系统
2. 导航到个人中心或用户信息页面
3. 等待页面加载完成（2秒）
4. 查看用户信息展示区域

### 验证
- 页面显示用户名"admin"
- 页面显示用户角色（ADMIN/管理员）
- 页面显示用户邮箱（如有）
- 页面显示用户手机号（如有）
- 页面显示创建时间（如有）
- 网络请求 GET /api/user/info 返回 200

### 清理
- 点击"退出登录"

---

## 用例2: 更新当前用户信息
**编号**: `user_info_update_normal_1`

### 步骤
1. 使用 buyer001/123456 登录系统
2. 导航到个人中心页面
3. 点击"编辑"或"修改信息"按钮
4. 修改昵称为"测试买家_已修改"
5. 修改手机号为"13800138001"
6. 点击"保存"按钮
7. 等待保存成功（2秒）
8. 刷新页面

### 验证
- 显示"保存成功"或"修改成功"提示
- 页面显示新的昵称"测试买家_已修改"
- 页面显示新的手机号"13800138001"
- 网络请求 PUT /api/user/info 返回 200

### 清理
- 将昵称改回"buyer001"
- 将手机号清空或改回原值
- 保存修改

---

## 用例3: 更新用户信息-字段验证失败
**编号**: `user_info_update_err_validation_1`

### 步骤
1. 使用 buyer001/123456 登录系统
2. 导航到个人中心页面
3. 点击"编辑"按钮
4. 输入无效的手机号"12345"（格式错误）
5. 点击"保存"按钮

### 验证
- 显示"手机号格式错误"或类似提示
- 未发起保存请求
- 用户信息未改变

### 清理
- 无需清理

---

## 用例4: 修改密码-正常流程
**编号**: `user_pwd_update_normal_1`

### 步骤
1. 使用 buyer001/123456 登录系统
2. 导航到个人中心或安全设置页面
3. 点击"修改密码"按钮
4. 输入旧密码：123456
5. 输入新密码：newpassword123
6. 确认新密码：newpassword123
7. 点击"确认修改"按钮
8. 等待修改成功（2秒）

### 验证
- 显示"密码修改成功"提示
- 自动退出登录或跳转到登录页
- 网络请求 PUT /api/user/password 返回 200

### 清理
- 使用新密码 newpassword123 登录
- 再次修改密码，将密码改回 123456
- 退出登录

---

## 用例5: 修改密码-旧密码错误
**编号**: `user_pwd_update_err_oldpwd_1`

### 步骤
1. 使用 buyer001/123456 登录系统
2. 导航到修改密码页面
3. 输入旧密码：wrongpassword
4. 输入新密码：newpassword123
5. 确认新密码：newpassword123
6. 点击"确认修改"按钮

### 验证
- 显示"旧密码错误"或"原密码不正确"提示
- 密码未被修改
- 仍然可以使用原密码登录

### 清理
- 无需清理

---

## 用例6: 修改密码-新密码不一致
**编号**: `user_pwd_update_err_mismatch_1`

### 步骤
1. 使用 buyer001/123456 登录系统
2. 导航到修改密码页面
3. 输入旧密码：123456
4. 输入新密码：newpassword123
5. 确认新密码：differentpassword
6. 点击"确认修改"按钮

### 验证
- 显示"两次密码不一致"或类似提示
- 未发起修改请求
- 密码未被修改

### 清理
- 无需清理

---

## 用例7: 用户登出
**编号**: `user_logout_normal_1`

### 步骤
1. 使用 admin/123456 登录系统
2. 验证登录成功，显示用户名
3. 点击页面右上角的用户头像或用户名
4. 在下拉菜单中点击"退出登录"
5. 等待页面跳转（2秒）

### 验证
- 跳转到登录页面
- 本地存储中的 Token 已被清除
- 网络请求 POST /api/user/logout 返回 200
- 访问受保护页面会重定向到登录页

### 清理
- 无需清理

---

# 第二部分：管理员接口

---

## 用例8: 获取用户列表-管理员
**编号**: `user_list_get_admin_1`

### 步骤
1. 使用 admin/123456 登录系统
2. 导航到用户管理页面
3. 等待用户列表加载（2秒）
4. 查看列表展示

### 验证
- 页面显示用户列表表格
- 表格包含列：用户名、角色、状态、创建时间等
- 列表中至少显示 admin、security001、buyer001 用户
- 网络请求 GET /api/user/list 返回 200
- 响应包含分页信息（total、current、size 等）

### 清理
- 无需清理

---

## 用例9: 获取用户列表-分页功能
**编号**: `user_list_pagination_1`

### 步骤
1. 使用 admin/123456 登录系统
2. 导航到用户管理页面
3. 查看当前页码和每页条数
4. 点击"下一页"按钮（如果有多页）
5. 或修改每页显示条数为 5
6. 观察列表变化

### 验证
- 分页组件正确显示当前页码
- 点击下一页后显示不同用户
- 修改每页条数后列表相应变化
- 网络请求包含 page 和 size 参数

### 清理
- 恢复默认分页设置

---

## 用例10: 获取用户列表-搜索过滤
**编号**: `user_list_search_1`

### 步骤
1. 使用 admin/123456 登录系统
2. 导航到用户管理页面
3. 在搜索框中输入"buyer"
4. 点击"搜索"按钮或按 Enter
5. 等待搜索结果（2秒）

### 验证
- 列表只显示用户名包含"buyer"的用户
- 搜索结果至少包含 buyer001
- 网络请求包含搜索参数（username=buyer 或 keyword=buyer）

### 清理
- 清空搜索条件
- 点击"重置"或清空搜索框

---

## 用例11: 获取用户列表-角色过滤
**编号**: `user_list_filter_role_1`

### 步骤
1. 使用 admin/123456 登录系统
2. 导航到用户管理页面
3. 在角色筛选下拉框中选择"BUYER"
4. 等待列表刷新（2秒）

### 验证
- 列表只显示角色为 BUYER 的用户
- 列表包含 buyer001
- 网络请求包含 role=BUYER 参数

### 清理
- 重置角色筛选为"全部"

---

## 用例12: 获取用户列表-非管理员无权限
**编号**: `user_list_err_permission_1`

### 步骤
1. 使用 buyer001/123456 登录系统
2. 尝试直接访问用户管理页面 URL（如 /user/list 或 /admin/users）
3. 或尝试通过 API 请求用户列表

### 验证
- 显示"无权限访问"或"权限不足"提示
- 或自动跳转到首页或 403 页面
- 网络请求返回 403 Forbidden

### 清理
- 退出登录

---

## 用例13: 获取用户详情-管理员
**编号**: `user_detail_get_admin_1`

### 步骤
1. 使用 admin/123456 登录系统
2. 导航到用户管理页面
3. 在用户列表中找到 buyer001 用户
4. 点击"查看"或用户名链接
5. 等待详情页面加载（2秒）

### 验证
- 显示用户详情页面或弹窗
- 包含用户名：buyer001
- 包含角色信息：BUYER
- 包含用户状态
- 包含创建时间
- 网络请求 GET /api/user/{id} 返回 200

### 清理
- 关闭详情弹窗或返回列表

---

## 用例14: 更新用户信息-管理员
**编号**: `user_admin_update_normal_1`

### 步骤
1. 使用 admin/123456 登录系统
2. 导航到用户管理页面
3. 找到 security001 用户，点击"编辑"按钮
4. 修改用户昵称为"安保人员_已修改"
5. 修改用户手机号为"13900139001"
6. 点击"保存"按钮
7. 等待保存成功（2秒）

### 验证
- 显示"保存成功"提示
- 用户列表中显示更新后的昵称
- 网络请求 PUT /api/user/{id} 返回 200

### 清理
- 将用户信息改回原值
- 保存修改

---

## 用例15: 更新用户角色-管理员
**编号**: `user_admin_update_role_1`

### 步骤
1. 使用 admin/123456 登录系统
2. 导航到用户管理页面
3. 找到测试用户，点击"编辑"按钮
4. 修改用户角色为 SELLER
5. 点击"保存"按钮
6. 等待保存成功（2秒）
7. 退出管理员登录
8. 使用该测试用户登录

### 验证
- 保存成功
- 该用户登录后能看到卖家相关菜单
- 网络请求 PUT /api/user/{id} 返回 200

### 清理
- 使用管理员账号登录
- 将用户角色改回原值

---

## 用例16: 更新用户状态-禁用用户
**编号**: `user_status_disable_1`

### 步骤
1. 使用 admin/123456 登录系统
2. 导航到用户管理页面
3. 找到 buyer001 用户
4. 点击"禁用"按钮或在状态开关上切换为禁用
5. 在确认弹窗中点击"确认"
6. 等待操作完成（2秒）
7. 退出管理员登录
8. 尝试使用 buyer001/123456 登录

### 验证
- 显示"操作成功"提示
- 用户列表中 buyer001 状态显示为"禁用"
- 使用 buyer001 登录时显示"账号已被禁用"或"账号异常"
- 网络请求 PUT /api/user/{id}/status?status=0 返回 200

### 清理
- 使用管理员账号登录
- 将 buyer001 状态改为启用（status=1）

---

## 用例17: 更新用户状态-启用用户
**编号**: `user_status_enable_1`

### 步骤
1. 使用 admin/123456 登录系统
2. 导航到用户管理页面
3. 找到已禁用的用户（如上一步禁用的 buyer001）
4. 点击"启用"按钮或在状态开关上切换为启用
5. 在确认弹窗中点击"确认"
6. 等待操作完成（2秒）
7. 退出管理员登录
8. 使用 buyer001/123456 登录

### 验证
- 显示"操作成功"提示
- 用户列表中 buyer001 状态显示为"启用"
- 使用 buyer001 可以正常登录
- 网络请求 PUT /api/user/{id}/status?status=1 返回 200

### 清理
- 无需清理

---

## 用例18: 重置用户密码-管理员
**编号**: `user_admin_reset_pwd_1`

### 步骤
1. 使用 admin/123456 登录系统
2. 导航到用户管理页面
3. 找到 security001 用户
4. 点击"重置密码"按钮
5. 在确认弹窗中点击"确认"
6. 等待操作完成（2秒）
7. 记录系统显示的新密码
8. 退出管理员登录
9. 使用 security001 和新密码登录

### 验证
- 显示"密码重置成功"提示
- 显示或可复制的新密码（如：随机8位字符）
- 网络请求 PUT /api/user/{id}/reset-password 返回 200
- 使用新密码可以成功登录

### 清理
- 使用管理员账号将 security001 密码重置为 123456

---

## 用例19: 删除用户-管理员
**编号**: `user_admin_delete_normal_1`

### 步骤
1. 使用 admin/123456 登录系统
2. 导航到用户管理页面
3. 找到要删除的测试用户（需要先创建一个测试用户）
4. 点击"删除"按钮
5. 在确认弹窗中点击"确认删除"
6. 等待删除完成（2秒）
7. 在用户列表中搜索该用户

### 验证
- 显示"删除成功"提示
- 用户列表中不再显示该用户
- 搜索该用户名返回空结果
- 网络请求 DELETE /api/user/{id} 返回 200

### 清理
- 无需清理（用户已被删除）

---

## 用例20: 删除用户-取消删除
**编号**: `user_admin_delete_cancel_1`

### 步骤
1. 使用 admin/123456 登录系统
2. 导航到用户管理页面
3. 找到 buyer001 用户
4. 点击"删除"按钮
5. 在确认弹窗中点击"取消"

### 验证
- 弹窗关闭
- 用户未被删除
- 用户列表中仍显示 buyer001
- 未发起删除请求

### 清理
- 无需清理

---

## 用例21: 删除用户-删除自己（应失败）
**编号**: `user_admin_delete_self_1`

### 步骤
1. 使用 admin/123456 登录系统
2. 导航到用户管理页面
3. 找到 admin 用户（当前登录用户）
4. 点击"删除"按钮
5. 在确认弹窗中点击"确认"

### 验证
- 显示"不能删除自己"或"无法删除当前登录用户"错误提示
- admin 用户未被删除
- 仍可正常使用系统

### 清理
- 无需清理

---

## 用例22: 获取不存在的用户详情
**编号**: `user_detail_err_notfound_1`

### 步骤
1. 使用 admin/123456 登录系统
2. 在地址栏直接输入不存在的用户 ID（如 /user/detail/999999）
3. 或通过 API 直接请求 GET /api/user/999999

### 验证
- 显示"用户不存在"错误提示
- 或跳转到用户列表页
- 网络请求返回 404 或业务码 500

### 清理
- 返回用户列表页面

---

## 用例23: 用户列表导出功能（如有）
**编号**: `user_list_export_1`

### 步骤
1. 使用 admin/123456 登录系统
2. 导航到用户管理页面
3. 点击"导出"或"导出Excel"按钮
4. 等待导出完成（5秒）

### 验证
- 浏览器下载用户列表文件
- 文件格式为 .xlsx 或 .csv
- 文件包含用户数据（用户名、角色、状态等）

### 清理
- 删除下载的测试文件

---

## 用例24: 用户管理批量操作（如有）
**编号**: `user_batch_operation_1`

### 步骤
1. 使用 admin/123456 登录系统
2. 导航到用户管理页面
3. 勾选多个用户（选择 security001 和 buyer001）
4. 点击"批量禁用"按钮
5. 在确认弹窗中点击"确认"
6. 等待操作完成（3秒）

### 验证
- 显示"批量操作成功"提示
- 被选中的用户状态都变为"禁用"
- 列表中显示更新后的状态

### 清理
- 选中被禁用的用户
- 点击"批量启用"恢复状态

---

## 用例25: 用户列表排序功能
**编号**: `user_list_sort_1`

### 步骤
1. 使用 admin/123456 登录系统
2. 导航到用户管理页面
3. 点击"创建时间"列头进行排序
4. 观察列表顺序变化
5. 再次点击切换升序/降序

### 验证
- 列表按照创建时间排序
- 点击后顺序反转
- 网络请求包含 sort 或 order 参数

### 清理
- 恢复默认排序

---

# 第三部分：权限边界测试

---

## 用例26: 非管理员访问用户详情
**编号**: `user_permission_err_detail_1`

### 步骤
1. 使用 security001/123456 登录系统
2. 尝试直接访问用户详情 API（GET /api/user/1）
3. 或访问用户管理页面

### 验证
- 返回 403 Forbidden
- 或显示"权限不足"提示
- 无法查看其他用户详情

### 清理
- 退出登录

---

## 用例27: 非管理员更新其他用户
**编号**: `user_permission_err_update_1`

### 步骤
1. 使用 security001/123456 登录系统
2. 尝试通过 API 更新其他用户信息（PUT /api/user/1）
3. 发送请求体包含修改的用户名

### 验证
- 返回 403 Forbidden
- 或显示"权限不足"提示
- 用户信息未被修改

### 清理
- 退出登录

---

## 用例28: 非管理员删除用户
**编号**: `user_permission_err_delete_1`

### 步骤
1. 使用 buyer001/123456 登录系统
2. 尝试通过 API 删除用户（DELETE /api/user/2）

### 验证
- 返回 403 Forbidden
- 或显示"权限不足"提示
- 用户未被删除

### 清理
- 退出登录

---

## 用例29: 未登录访问用户接口
**编号**: `user_auth_err_unauth_1`

### 步骤
1. 确保未登录状态（清除 Token）
2. 尝试访问用户列表页面
3. 尝试访问个人中心页面

### 验证
- 自动跳转到登录页面
- 或显示"请先登录"提示
- API 返回 401 Unauthorized

### 清理
- 无需清理
